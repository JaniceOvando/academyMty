<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.curso</groupId>
	<artifactId>mockito</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>mockito</name>
	<description>Probar un servicio cuando del colaborador solo conocemos la interfaz</description>

	<properties>
		<maven.compiler.release>21</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<junit.version>6.1.3</junit.version>
		<mockito.version>5.23.0</mockito.version>
		<surefire.version>3.5.6</surefire.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- El junit-bom va PRIMERO y por eso gana: mockito-junit-jupiter
			     5.23.0 arrastra junit-jupiter-api 5.13.4, pero este BOM fija
			     6.1.3 en todo el arbol. Compruebalo:
			         ./mvnw dependency:tree "-Dincludes=org.junit*:*"          -->
			<dependency>
				<groupId>org.junit</groupId>
				<artifactId>junit-bom</artifactId>
				<version>${junit.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-bom</artifactId>
				<version>${mockito.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- mock(), when(), verify(), ArgumentCaptor... -->
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- Solo aporta MockitoExtension, para @ExtendWith, @Mock e @InjectMocks.
		     Sin esto, @Mock no se rellena y salen NullPointerException. -->
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<!-- OJO AL SCOPE: las tres dependencias son <scope>test</scope>.
	     Mockito NO viaja al artefacto de produccion, y por eso Principal.java
	     (que vive en src/main/java) no puede usar mock(). Ver su comentario. -->

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${surefire.version}</version>
				<configuration>
					<!-- Carga el agente de Mockito por adelantado. Sin esta linea,
					     cada corrida en Java 21 escupe:
					       Mockito is currently self-attaching to enable the
					       inline-mock-maker...
					       WARNING: A Java agent has been loaded dynamically

					     OJO CON COMO SE ESCRIBE LA RUTA. Lo natural aqui seria
					     ${org.mockito:mockito-core:jar}, la propiedad que publica
					     el maven-dependency-plugin. NO LA USES: esa propiedad solo
					     existe DURANTE el build de Maven. Eclipse lee este argLine
					     al hacer Run As > JUnit Test, pero no ejecuta el plugin que
					     la define, asi que le pasa al JVM el texto crudo y revienta:

					       Error opening zip file or JAR manifest missing
					       Error occurred during initialization of VM
					       agent library failed Agent_OnLoad: instrument

					     settings.localRepository y mockito.version salen del modelo
					     del POM, y esas Eclipse si las resuelve. Funciona en los dos
					     sitios y en cualquier maquina. -->
					<argLine>-javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar -Xshare:off</argLine>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
